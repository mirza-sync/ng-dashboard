# ng Dashboard

This project was generated with [Angular CLI](https://github.com/angular/angular-cli) version 14.2.13.

**Tech-stack**: Angular 14, TailwindCSS v3, Chart.js, Electron, PouchDB

## Development server

Run `ng serve` for a dev server. Navigate to `http://localhost:4200/`. The application will automatically reload if you change any of the source files.

Use the below credential to authenticate the user:

- email: user@aemenersol.com
- password: Test@123

## Run the Electron App

Use below command to run the desktop preview

```
npm run electron
```

## Compile Desktop app

Use below command to compile the executable (Only tested on Windows)

```
npm run desktop:dist
```
